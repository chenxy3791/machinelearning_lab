# Basic MNIST Example

```bash
pip install -r requirements.txt
python main.py                           # Using default parameters to run
python main.py --epoch 2 --batch-size 64 # to specify some command-line parameters
# CUDA_VISIBLE_DEVICES=2 python main.py  # to specify GPU id to ex. 2

```
